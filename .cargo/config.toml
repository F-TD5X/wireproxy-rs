[target.x86_64-unknown-linux-musl]
# curve25519-dalek 4.0.0-rc.3 selects its SIMD backend on x86_64, but that
# release still uses the removed `stdsimd` feature on recent nightly compilers.
rustflags = ["--cfg=curve25519_dalek_backend=\"serial\""]
